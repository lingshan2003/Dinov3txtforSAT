# 面向遥感视觉理解的领域图文对齐：科研背景与研究设计

更新日期：2026-09-08。本文回答“为什么研究、研究什么、什么证据足以支持结论”。具体模型参数、训练命令和实时进度见[最新交接](docs/PREPARE_HANDOFF.md)，系统开发约束见[开发架构](docs/DEVELOPMENT_ARCHITECTURE.md)。保留原文件名以兼容已有引用。

## 1. 研究背景与问题定义

遥感中的文本检索、开放词汇识别和弱监督定位，需要建立影像内容与领域语言之间的对应关系。困难既来自视觉分布，也来自语言：尺度、视角和地物结构与日常照片不同；描述还可能包含设施用途、细粒度属性和空间关系，其中部分信息未必能从图像中直接看见。

视觉自监督模型提供了可复用的视觉表示，但视觉表示本身并不等于语言接口。图文对齐方法把图像与文本映射到可比较的表示空间，使文本可以用于检索或分类。项目以 DINO 系列和 dino.txt 为技术起点，研究如何在有限算力下完成遥感领域适配。

核心问题是：

> 如何获得可信的遥感图文对应关系，并在领域适配过程中保持已有能力？视觉预训练域、文本表达和适配范围分别有什么贡献？

本项目的价值在于控制变量、建立可复现的评测和解释成功或失败的条件。单一训练 loss 下降、替换一个 backbone，或加入更多模块，都不足以单独构成科研贡献。

## 2. 三组研究问题及优先级

### RQ1：视觉预训练域与遥感图文适配如何共同影响能力？

首先建立能稳定优化且保持外部能力的候选，再进行以下对照：

| 视觉初始化 | 不做遥感适配 | 做相同的遥感图文适配 |
| --- | --- | --- |
| 通用视觉域（Web） | 通用初始化基线 | 测量该初始化上的适配增益与遗忘 |
| 遥感视觉域（SAT） | 迁移通用对齐头的初始化诊断 | 测量另一视觉初始化上的适配增益与遗忘 |

同一行比较适配前后；两行比较在相同协议下，不同视觉初始化如何影响结果。图片、文本、可训练范围、训练预算和评测协议应匹配；不同 backbone 必需的预处理差异应明确记录。

当前 SAT 路径使用通用 dino.txt 对齐头迁移初始化，存在兼容性混杂。因此两行之差不能直接解释为“纯粹的视觉域收益”，也不能把 SAT 初始化称为官方 SAT-dino.txt。

这里的“遥感图文适配”指利用遥感图文监督优化对齐关系，**不要求更新文本编码器**。当前 image-adapter-only 候选也属于该研究范围；旧文档将语言域匹配与解冻文本塔绑定的表述不再适用。

### RQ2：什么样的文本表达适合遥感对齐？

在相同图片与 split 上比较原始短描述、统一模板、结构化属性和层级文本。首个可执行对照是 SkyScript `title_raw` 与带统一前缀的文本；结构化、层级文本仍是后续假设。

实验需要区分文本内容、模板、长度和信息来源。不能同时更换图片筛选结果与文本表达后，把差异全部归因于文本质量。生成或整理结构化文本时要保留来源，审查图像不可见属性；不能把未经验证的描述当作额外真值。

### RQ3：全局对齐能否扩展到可信的局部语义对应？

全局图文检索成立后，再研究 patch 与短语的对应、弱局部监督或多粒度目标。必须有独立的局部评价协议；全局 loss 下降和热力图看起来合理都不能替代定位或分割证据。

当前 adapter 仅作用于全局图像 embedding。RQ3 属于可选扩展，不是当前已完成能力，也不是最低毕业验收的前提。

## 3. 数据路线与角色

| 数据 | 当前角色 | 使用限制 |
| --- | --- | --- |
| SkyScript language-polished top30，images2+images3 | 主训练数据与同源 held-out validation | `title_raw` 精确去重后拆分；文本互斥不能证明地理或图像内容互斥 |
| ChatEarthNet | 历史工程验证与失败分析 | 不再作为当前默认训练集；旧结果不可与新协议直接比较 |
| RSICD val | 开发阶段外部保持集 | 已在历史开发中观察过，不能称为未触碰测试集 |
| RSICD test、全量 EuroSAT | 已使用过的历史外部评测 | 不再循环用于调参；最终报告必须披露使用历史 |
| 其他独立遥感检索或分类数据 | 后续泛化评测候选 | 先核实来源重叠、许可、split 和测试使用计划，再纳入实验 |

切换到 SkyScript 的工作假设是：更短的地物描述、显式去模板化和重复控制，可能让初期对齐更可控。这尚不是“SkyScript 优于 ChatEarthNet”的因果结论，因为切换时还改变了更新范围、queue 和增强。

精确 caption 去重减少已知重复文本成为负样本的问题，但不解决同义文本、近重复图像或同区域采样。每种 caption 只取一个样本也改变了频率分布。去重子集上的效果不能直接推广到原始百万级分布。

具体 CSV 版本、样本数、hash、清洗策略和风险只维护在[交接的数据协议](docs/PREPARE_HANDOFF.md#4-当前数据协议skyscript)，不在本文另列一套数值。

## 4. 方法选择原则

先用最小可训练范围验证数据和监督是否有效。冻结已有表示、训练轻量适配模块是当前候选，后续是否解冻官方层、引入表示保持约束或改变对齐目标，由证据决定。

每次增加复杂度必须对应一个可检验的假设：

| 变化 | 要回答的问题 | 必须同时观察 |
| --- | --- | --- |
| 小模块适配或改变容量 | 更新能力是否足够、是否过拟合 | 同域检索与外部保持 |
| 表示保持约束 | 能否减少适配造成的遗忘 | 适配收益是否被抵消 |
| 模板或结构化文本 | 表达形式是否影响匹配 | 同图对照、长度与可见性 |
| 多正例或负样本处理 | 重复语义是否损害对比目标 | 负样本身份及指标语义 |
| 局部对齐 | 是否改善空间语义对应 | 独立局部任务评测 |

梯度累积扩大优化器有效 batch，不扩大单次对比 softmax 的候选集合。历史负样本队列也不等价于同一时刻编码的大 batch；不能用这两种操作掩盖负样本问题。

## 5. 实验与证据标准

1. **工程正确性**：输入可追溯、冻结边界正确、初始化一致、训练有限值、checkpoint 可验证和恢复。
2. **同源学习信号**：固定 held-out loss 与全局双向 retrieval 都改善；明确 loss 分组、检索候选池与正例定义。
3. **外部保持性**：在开发保持集上比较自身初始化，提前定义可接受退化范围。
4. **稳定性**：至少三个 seed，报告逐 seed 结果、均值和标准差，并检查更长日程是否反转。
5. **泛化与论文结论**：提前冻结最终评测协议，披露测试集观察历史；开发集上的选择不能当作独立泛化证据。

检索报告 I→T/T→I R@1/5/10、mean recall 和 median rank；分类固定 prompt、类别映射与图像处理。若数据存在多个有效正例，评价必须反映这一关系。消融一次只改变目标变量，数据规模对照优先使用固定且嵌套的训练子集。

当前单 seed 短训练的积极信号足以支持继续核验候选，尚不足以支持 Web/SAT 优劣、跨数据源提升或局部能力改善。实时数值与具体通过门槛见交接。

## 6. 最小研究交付与扩展边界

最低交付包括：可复现工程闭环、清楚的数据与评测协议、领域适配前后的对照、稳定性和保持性分析，以及对失败条件的解释。在候选通过验证后完成 RQ1，并按资源推进 RQ2；RQ3 作为扩展。

有价值的结论可以是某种方法在明确条件下有效，也可以是视觉域迁移不自动带来语言收益，或同源改善伴随外部遗忘。结论必须由相应对照支持，不能预先承诺 SOTA 或把尚未开展的实验列为贡献。

正式阶段沿用 M0–M7，具体样本规模与当前 Gate S0–S3 统一在交接维护；本文不再保留已失效的逐月计划、首轮 ChatEarthNet 下载安排或固定解冻策略。

## 7. 核心阅读线索

以下保留原研究规划中的核心文献入口，用于理解方法来源；本文重组不构成对全部相关工作的新一轮综述。

- [DINOv2 Meets Text（CVPR 2025）](https://openaccess.thecvf.com/content/CVPR2025/html/Jose_DINOv2_Meets_Text_A_Unified_Framework_for_Image-_and_Pixel-Level_CVPR_2025_paper.html)：图像级与像素级语言对齐的主要方法起点。
- [DINOv3 官方仓库](https://github.com/facebookresearch/dinov3)：当前固定版本所依赖的视觉模型与 dino.txt 实现；项目实际版本见交接。
- [RemoteCLIP](https://doi.org/10.1109/TGRS.2024.3390838)、[RS5M / GeoRSCLIP](https://doi.org/10.1109/TGRS.2024.3449154)：遥感图文模型与领域数据对齐的对照阅读。
- [ChatEarthNet](https://essd.copernicus.org/articles/17/1245/2025/)：理解历史数据生成与文本结构，服务失败分析。

SkyScript 当前输入以交接记录的完整 CSV 文件名、选择清单和 hash 标识；正式论文引用与数据许可信息需随所用上游版本补齐。未实施的模型清单及扩展论文不再作为当前工作承诺。
